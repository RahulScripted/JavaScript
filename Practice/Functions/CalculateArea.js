// Develop a function to compute and return the area of a square, given its side.




const side = parseInt(input);
function calculateArea(side) {
    return side * side;
}

console.log(calculateArea(side));